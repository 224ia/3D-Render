package UI.Elements;

import UI.UIElement;

import java.awt.*;

public class UILabel extends UIElement {
    public UILabel(int x, int y, int width, int height, Color bgColor) {
        super(x, y, width, height, bgColor);
    }
}
